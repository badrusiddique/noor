import type { ExpoConfig } from 'expo/config';

const config: ExpoConfig = {
  name: 'Noor',
  slug: 'noor',
  version: '0.1.0',
  scheme: 'noor',
  orientation: 'portrait',
  icon: './assets/images/icon.png',
  userInterfaceStyle: 'automatic',
  newArchEnabled: true,
  splash: {
    image: './assets/images/splash.png',
    resizeMode: 'contain',
    // Brand navy from the icon backdrop, so launch -> splash -> first
    // frame stays visually continuous instead of flashing parchment under
    // the dark-themed icon during the resize fit.
    backgroundColor: '#131f2a',
  },
  assetBundlePatterns: ['assets/fonts/*', 'assets/db/*', 'assets/audio/**/*', 'assets/images/*'],
  // expo-updates is required by EAS Build's dev-client + production
  // pipelines, but we deliberately do NOT ship OTA updates in v1.0 (see
  // docs/BACKEND-HONESTY.md). The url declares the project's update
  // channel for build-time identity only; runtime checks stay disabled
  // until we revisit the OTA decision post-1.0.
  updates: {
    url: 'https://u.expo.dev/a57d0dd9-ca31-4d18-af11-ce5389a6274d',
    enabled: false,
  },
  runtimeVersion: {
    policy: 'appVersion',
  },
  ios: {
    supportsTablet: true,
    bundleIdentifier: 'app.noor.quran',
    infoPlist: {
      UIBackgroundModes: ['audio'],
      ITSAppUsesNonExemptEncryption: false,
    },
  },
  android: {
    package: 'app.noor.quran',
    adaptiveIcon: {
      foregroundImage: './assets/images/adaptive-icon.png',
      // Matches the icon backdrop so Android's circular/squircle masks
      // don't reveal a parchment edge around the rendered logo.
      backgroundColor: '#131f2a',
    },
    permissions: [],
  },
  web: {
    favicon: './assets/images/favicon.png',
    bundler: 'metro',
  },
  plugins: ['expo-router', 'expo-font', 'expo-localization'],
  experiments: {
    typedRoutes: true,
  },
  extra: {
    router: {
      origin: false,
    },
    eas: {
      // Generated by `eas init` for the badrusiddique account; required for
      // EAS Build / Update to associate this app with the cloud project.
      // Safe to commit (it's a public identifier, not a secret).
      projectId: 'a57d0dd9-ca31-4d18-af11-ce5389a6274d',
    },
  },
  owner: 'badrusiddique',
};

export default config;
